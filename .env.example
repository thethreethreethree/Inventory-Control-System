# Copy to .env (repo root). Defaults match docker-compose.yml.
DATABASE_URL=postgresql://ics:ics_dev_password@localhost:5432/ics
API_PORT=4000
WEB_PORT=5173
NODE_ENV=development
# Secret for signing session tokens. Use a long random value in production.
AUTH_SECRET=dev-only-change-me-secret
